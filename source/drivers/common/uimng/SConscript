# -*- Mode: Python -*-

Import('env')

if env['ARCH'] == 'i586':
	env.EscapeCXXProg('sbin', target = 'uimng', source = env.Glob('*.cc'), LIBS = ['ipc','vbetext'])
else:
	env.EscapeCXXProg('sbin', target = 'uimng', source = env.Glob('*.cc'), LIBS = ['ipc'])
