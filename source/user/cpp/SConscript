# -*- Mode: Python -*-

Import('env')

if env['ARCH'] == 'i586':
	env.SConscript(Glob('*/SConscript'), 'env')
else:
	# TODO mmix-ld is unable to link some c++ apps
	for f in ['init', 'less', 'libcpptest', 'ls', 'ps', 'sort', 'ts']:
		env.SConscript(f + '/SConscript', 'env')
